name: emergency_app
description: Emergency Alert System - User Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.1

  # HTTP Requests
  http: ^1.1.0

  # Location
  geolocator: ^10.1.0
  geocoding: ^2.1.1

  # Google Maps
  google_maps_flutter: ^2.5.0

  # Firebase - COMMENTED OUT FOR NOW
  # Uncomment when you're ready to add Firebase
  # firebase_core: ^2.24.2
  # firebase_messaging: ^14.7.9

  # Local Storage
  shared_preferences: ^2.2.2

  # UI Components
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # Add assets if needed
  # assets:
  #   - assets/images/
  #   - assets/sounds/